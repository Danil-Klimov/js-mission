include _mixins
block vars

doctype
html(lang="ru")
	head
		meta(charset="utf-8")
		meta(http-equiv="X-UA-Compatible" content="IE=edge")
		meta(name="viewport" content="width=device-width, initial-scale=1.0")
		link(href=`css/style.css?v=${Date.now()}` rel="stylesheet")
		block title
	body
		block body-start
		block content
		block scripts
			//script(src=`js/libs.min.js?v=${Date.now()}`)
			script(src=`js/main.js?v=${Date.now()}`)
		block body-end